# -*- mode: python -*-
a = Analysis(['../src/jcd.py'],
             pathex=['C:\\Users\\bewigo\\Documents\\workspace-jcdebug\\JCDCmd\\pyinstaller'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='jcd.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='jcd')
